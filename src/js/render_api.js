const movielist = document.querySelector('[data-list]');
const modal = document.querySelector('[data-modal]');
const library = document.querySelector('[data-library]');

export default class RenderApi {
	constructor() {}
}
